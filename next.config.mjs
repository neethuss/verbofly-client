/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'talktrek.s3.ap-southeast-2.amazonaws.com','assets.aceternity.com'],
  },
};

export default nextConfig;
